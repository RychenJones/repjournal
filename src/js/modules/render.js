// /src/js/modules/render.js
// RepJournal — DOM-building helpers
// Functions that take data and return DOM elements/markup, reusable
// across pages. Empty for now — workout history and log-workout will
// add things like renderWorkoutCard() and renderSetRow() here.